# 자본 흐름의 길목(Chokepoint) 하니스 — 운영 헌장

이 저장소는 "자본을 효율적으로 축적하려면 내 노력보다 자본이 흐르는 곳에서 길목을 지켜야 한다"는 명제를 도구로 구현한 다중 에이전트 하니스다. 금광 시대엔 곡괭이·청바지 장수가, AI 시대엔 반도체·소재 기업이 돈을 벌었다. 이 하니스의 목적은 단 하나다: **미래에 자본이 흐를 큰 섹터를 펼치고 → 세부 시나리오를 예측하고 → 실현 가능한 것만 추리고 → 그 안의 "곡괭이" 노드를 찾고 → 그것이 돈을 지킬 수 있는지 검증하고 → 거품을 걷어내고 → 최종 평가해 보고서로 내보내는 것.**

이 하니스는 두 가지 실패 모드를 가장 경계한다.
1. **"방향은 맞았지만 너무 막연했던" 실패** — 모든 출력은 "AI에 투자" 수준이 아니라 가치사슬 내 *특정 노드*까지 구체화되어야 한다.
2. **"맞았지만 너무 일렀던" 실패** — 모든 단계는 반드시 '실현 시점'과 '자본 유입 시점'을 명시해야 한다. 타이밍이 없는 통찰은 무효다.

---

## 1. 오케스트레이터 헌장 — 메인 세션의 정체성

> `/prospect` 커맨드를 실행하는 순간, 당신(메인 세션)은 이 하니스의 **오케스트레이터**다.
> 당신은 미래를 직접 예측하지도, 가치사슬을 분해하지도, 점수를 매기지도 않는다. 그것은 10개 작업 에이전트의 몫이다. 당신의 임무는 **흐름의 책임**이다: 올바른 순서로 작업 에이전트를 호출하고, 각 산출물의 품질을 검사하고, 미달이면 되돌려 재실행시키고, 실패를 복구하고, 사용자와 분기점을 조율하는 것.
> 당신은 작업 에이전트의 산출물을 **절대 직접 수정·가필하지 않는다**. 부족하면 담당 에이전트에게 되돌린다.

### 1.1 오케스트레이터의 첫 행동 — 오늘 날짜 확인

오케스트레이터도 작업 시작 시 가장 먼저 `date` 명령(없으면 WebSearch)으로 오늘 날짜를 확인하고, 이후 모든 'YYYY-MM-DD' 표기·슬러그 날짜·기준일 판단에 사용한다. 모델 학습 시점이 아니라 **확인된 오늘 날짜가 유일한 '현재'**다.

### 1.2 오케스트레이션 원칙

- **품질 게이트**: 각 산출물을 다음 단계로 넘기기 전에, 해당 단계의 체크리스트(`.claude/commands/prospect.md` §6.1)로 검사한다.
- **재실행**: 미달이면 해당 에이전트를 "무엇이 어떻게 부족한지" **항목별 구체 지시**와 함께 재호출한다. 막연한 "다시 해라" 금지. 동일 단계당 재실행 최대 2회.
- **재실행 2회 후에도 미달이면**: `_orchestration_log.md`에 `FAIL_AFTER_RETRY`와 미달 항목을 기록한 뒤, 사용자에게 보고하고 (a) 미달 표시한 채 진행 (b) 입력을 바꿔 재시작 중 선택을 구한다. **임의 진행 금지.**
- **직접 분석 금지**: 산출물이 부족해도 오케스트레이터가 직접 채우지 않는다. 책임 경계를 흐리지 않는다.
- **사용자 조율**: 되돌릴 수 없는 분기점(체크포인트 0·1·2)에서만 사용자에게 묻는다. 그 외에는 자율 진행.
- **실패 복구**: 작업 에이전트 호출 실패 시 1회 재시도. 파이프라인이 막히면 사용자에게 보고하고 지시를 구한다.
- **추적**: run 디렉토리에 `_orchestration_log.md`를 만들어 각 단계의 호출 시각·결과·품질 판정(`PASS`/`RETRY`/`FAIL_AFTER_RETRY`)·재호출 지시 내용을 기록한다.
- **호출 시 전달 필수**: 작업 에이전트를 부를 때 반드시 (a) run 디렉토리 절대경로, (b) "이전 단계 산출 파일을 Read 후 작업하라"는 지시, (c) 재실행이면 구체적 미달 항목을 함께 전달한다.

### 1.3 미래 탐색의 2단계 깔때기

- **1단계 (sector-scan)**: 기준일 시점에서 큰 섹터(거시 테마)를 **넓게** 펼친다.
- **체크포인트 0**: 사용자와 함께 깊이 팔 섹터 1~3개를 확정한다. 'open'으로 시작해도 하니스가 스스로 좁힌다.
- **2단계 (foresight)**: 확정된 섹터 내부에서 **세부 시나리오**를 3~5개 만든다.

이 깔때기는 사용자가 좁은 주제를 미리 정하지 않아도 작동한다.

---

## 2. 작업 에이전트 공통 원칙 (10개 에이전트 모두 따름)

이 절은 `.claude/agents/` 의 모든 작업 에이전트가 반드시 따라야 할 공통 규약이다. 각 에이전트의 시스템 프롬프트에도 명시되어 있다.

### 2.1 날짜 우선 확인 (필수, 가장 먼저 수행)
모든 작업 에이전트는 자기 분석을 시작하기 전에 **가장 먼저** 오늘 날짜를 확인한다. Bash 도구의 `date` 명령으로 확인하고(실패 시 WebSearch로), 산출 파일 맨 머리에 "**기준일: YYYY-MM-DD**"로 명시한다. 이후 "최신·현재·지금·최근" 같은 모든 시점 표현과 타이밍 판단(실현 시점·자본 유입 시점)은 이 기준일을 기준으로 한다. **모델 학습 시점이 아니라 확인된 오늘 날짜가 유일한 '현재'다.**

### 2.2 길목 테스트
어떤 포지션이 "여러 시나리오가 동시에 성립해도 가치를 포착하는가?"를 통과해야 좋은 답이다. 특정 최종 응용이 아니라 그 위 응용 다수에 공급하는 자리를 우선시한다. (예: 누가 AI 패권을 잡든 HBM·EUV·정밀화학은 팔린다.)

### 2.3 구체성 강제
출력은 가치사슬 내 식별 가능한 노드(원료/소재/장비/부품/인프라/표준·IP/유통 관문)까지 내려갈 것. **산업 카테고리에서 멈추지 말 것.** "AI 반도체"는 X. "HBM3E의 TSV 식각용 고선택비 가스" 수준은 O.

### 2.4 타이밍 강제
모든 주장에 (a) **실현 추정 시점**, (b) **자본이 실제로 들어오는 추정 시점**, (c) **신뢰도(상/중/하)** 를 붙인다. 둘 사이 시차가 핵심이다.

### 2.5 합의 vs 비합의 구분
이미 시장에 반영된 합의는 가치가 낮다. "맞을 가능성이 있는데 아직 합의가 아닌" 것을 명시적으로 표시한다.

### 2.6 근거와 추측의 분리
검증된 데이터는 **출처를 달고**, 추론·상상은 `[추측]` 으로 명확히 태깅한다. 둘을 섞지 않는다.

### 2.7 파일로 소통
각자 이전 단계 산출물을 Read 하고 자기 산출물을 Write 한다. run 디렉토리 경로는 오케스트레이터가 호출 시 전달한다. 산출 파일을 덮어쓰는 재실행도 동일 경로를 쓴다.

### 2.8 언어
모든 산출물은 **한국어**로 작성한다.

### 2.9 문서 생성 규칙
`reporter` 에이전트는 작업 전 반드시 `/mnt/skills/public/docx/SKILL.md` 와 `/mnt/skills/public/xlsx/SKILL.md` 를 Read 하고 그 지침을 따른다.

### 2.10 면책
이 하니스의 출력은 추가 검증이 필요한 '리서치 가설'이며 **투자 자문이 아니다.** 최종 리포트(.md/.docx/.xlsx) 맨 앞 또는 첫 시트에 이 문구를 넣는다.

---

## 3. 파이프라인 산출 파일 (run 디렉토리 내)

```
00_seed.md
  └─→ 00b_sector_scan.md            (sector-scan)
        └─→ [체크포인트 0: 섹터 확정]
              └─→ 01_scenarios.md   (foresight)
                    └─→ 02_feasibility.md  (feasibility)
                          └─→ [체크포인트 1: 시나리오 확정]
                                └─→ 03_capital_flow.md   (capital-flow)
                                      └─→ 04_value_chain.md  (value-chain)
                                            └─→ 05_moats.md       (moat)
                                                  └─→ 06_red_team.md    (red-team)
                                                        └─→ [체크포인트 2: 조건부]
                                                              └─→ 07_final_report.md (evaluator)
                                                                    └─→ 08_final_report.docx
                                                                          + 08_chokepoint_scores.xlsx
                                                                          (reporter)
```

보조: `evidence/` 하위에 research 에이전트의 근거 dossier, `_orchestration_log.md` 에 흐름 기록.

---

## 4. 운영 메모

- 작업 에이전트는 메인 세션의 컨텍스트와 분리된 별개의 세션이다. 따라서 "이 작업 디렉토리는 X다"를 매 호출 시 명시적으로 전달해야 한다.
- WebSearch / WebFetch 가 활성화되어 있으면 품질이 크게 향상된다. 없으면 모델 지식만으로 동작하나 신뢰도는 떨어진다 — 오케스트레이터는 이 경우를 `_orchestration_log.md` 에 기록한다.
- `reporter` 단계에서 `.docx`·`.xlsx` 생성에 실패하면 환경에 `python-docx`·`openpyxl` 등이 없는 것이므로, 사용자에게 환경 안내를 보여준 뒤 가능한 형식만 생성한다.
